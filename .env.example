# This project runs on Cloudflare Workers and uses bindings for runtime storage.
# For local credential overrides, copy `.dev.vars.example` to `.dev.vars`.
#
# Example:
# ADMIN_USERNAME=your-admin-username
# ADMIN_PASSWORD=your-admin-password
